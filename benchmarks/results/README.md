# Benchmark results

Store one markdown file per experiment, e.g.:

- `2026-05-16-nginx-alpine-r1.md` — first run, note QEMU and package versions in the header.
- Prefer copying [`../TEMPLATE.md`](../TEMPLATE.md) and filling tables; attach raw `wrk`/log files in the same directory if small, or link to external storage for large artifacts.

Do not commit secrets, customer data, or licensed binaries you are not allowed to redistribute.
